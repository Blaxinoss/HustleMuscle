import react from 'React';

const UseMessage = (initMessage) => {

    const [message, setMessage] = useState(initMessage)


    return (
        <div className="top-0">

    </div>)

export default UseMessage;